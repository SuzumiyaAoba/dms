# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# CORS
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info

# Storage
# Storage backend: filesystem or s3
STORAGE_TYPE=filesystem
# Base path for file storage (used when STORAGE_TYPE=filesystem)
STORAGE_PATH=./storage/documents
